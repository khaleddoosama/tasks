import { n as __commonJSMin, r as __toESM, t as require_react } from "./react-DTjSBzeh.js";
import { t as require_react_dom } from "./react-dom-EcJP2q-5.js";
//#region node_modules/react/cjs/react-jsx-runtime.development.js
/**
* @license React
* react-jsx-runtime.development.js
*
* Copyright (c) Meta Platforms, Inc. and affiliates.
*
* This source code is licensed under the MIT license found in the
* LICENSE file in the root directory of this source tree.
*/
var require_react_jsx_runtime_development = /* @__PURE__ */ __commonJSMin(((exports) => {
	(function() {
		function getComponentNameFromType(type) {
			if (null == type) return null;
			if ("function" === typeof type) return type.$$typeof === REACT_CLIENT_REFERENCE ? null : type.displayName || type.name || null;
			if ("string" === typeof type) return type;
			switch (type) {
				case REACT_FRAGMENT_TYPE: return "Fragment";
				case REACT_PROFILER_TYPE: return "Profiler";
				case REACT_STRICT_MODE_TYPE: return "StrictMode";
				case REACT_SUSPENSE_TYPE: return "Suspense";
				case REACT_SUSPENSE_LIST_TYPE: return "SuspenseList";
				case REACT_ACTIVITY_TYPE: return "Activity";
			}
			if ("object" === typeof type) switch ("number" === typeof type.tag && console.error("Received an unexpected object in getComponentNameFromType(). This is likely a bug in React. Please file an issue."), type.$$typeof) {
				case REACT_PORTAL_TYPE: return "Portal";
				case REACT_CONTEXT_TYPE: return type.displayName || "Context";
				case REACT_CONSUMER_TYPE: return (type._context.displayName || "Context") + ".Consumer";
				case REACT_FORWARD_REF_TYPE:
					var innerType = type.render;
					type = type.displayName;
					type || (type = innerType.displayName || innerType.name || "", type = "" !== type ? "ForwardRef(" + type + ")" : "ForwardRef");
					return type;
				case REACT_MEMO_TYPE: return innerType = type.displayName || null, null !== innerType ? innerType : getComponentNameFromType(type.type) || "Memo";
				case REACT_LAZY_TYPE:
					innerType = type._payload;
					type = type._init;
					try {
						return getComponentNameFromType(type(innerType));
					} catch (x) {}
			}
			return null;
		}
		function testStringCoercion(value) {
			return "" + value;
		}
		function checkKeyStringCoercion(value) {
			try {
				testStringCoercion(value);
				var JSCompiler_inline_result = !1;
			} catch (e) {
				JSCompiler_inline_result = !0;
			}
			if (JSCompiler_inline_result) {
				JSCompiler_inline_result = console;
				var JSCompiler_temp_const = JSCompiler_inline_result.error;
				var JSCompiler_inline_result$jscomp$0 = "function" === typeof Symbol && Symbol.toStringTag && value[Symbol.toStringTag] || value.constructor.name || "Object";
				JSCompiler_temp_const.call(JSCompiler_inline_result, "The provided key is an unsupported type %s. This value must be coerced to a string before using it here.", JSCompiler_inline_result$jscomp$0);
				return testStringCoercion(value);
			}
		}
		function getTaskName(type) {
			if (type === REACT_FRAGMENT_TYPE) return "<>";
			if ("object" === typeof type && null !== type && type.$$typeof === REACT_LAZY_TYPE) return "<...>";
			try {
				var name = getComponentNameFromType(type);
				return name ? "<" + name + ">" : "<...>";
			} catch (x) {
				return "<...>";
			}
		}
		function getOwner() {
			var dispatcher = ReactSharedInternals.A;
			return null === dispatcher ? null : dispatcher.getOwner();
		}
		function UnknownOwner() {
			return Error("react-stack-top-frame");
		}
		function hasValidKey(config) {
			if (hasOwnProperty.call(config, "key")) {
				var getter = Object.getOwnPropertyDescriptor(config, "key").get;
				if (getter && getter.isReactWarning) return !1;
			}
			return void 0 !== config.key;
		}
		function defineKeyPropWarningGetter(props, displayName) {
			function warnAboutAccessingKey() {
				specialPropKeyWarningShown || (specialPropKeyWarningShown = !0, console.error("%s: `key` is not a prop. Trying to access it will result in `undefined` being returned. If you need to access the same value within the child component, you should pass it as a different prop. (https://react.dev/link/special-props)", displayName));
			}
			warnAboutAccessingKey.isReactWarning = !0;
			Object.defineProperty(props, "key", {
				get: warnAboutAccessingKey,
				configurable: !0
			});
		}
		function elementRefGetterWithDeprecationWarning() {
			var componentName = getComponentNameFromType(this.type);
			didWarnAboutElementRef[componentName] || (didWarnAboutElementRef[componentName] = !0, console.error("Accessing element.ref was removed in React 19. ref is now a regular prop. It will be removed from the JSX Element type in a future release."));
			componentName = this.props.ref;
			return void 0 !== componentName ? componentName : null;
		}
		function ReactElement(type, key, props, owner, debugStack, debugTask) {
			var refProp = props.ref;
			type = {
				$$typeof: REACT_ELEMENT_TYPE,
				type,
				key,
				props,
				_owner: owner
			};
			null !== (void 0 !== refProp ? refProp : null) ? Object.defineProperty(type, "ref", {
				enumerable: !1,
				get: elementRefGetterWithDeprecationWarning
			}) : Object.defineProperty(type, "ref", {
				enumerable: !1,
				value: null
			});
			type._store = {};
			Object.defineProperty(type._store, "validated", {
				configurable: !1,
				enumerable: !1,
				writable: !0,
				value: 0
			});
			Object.defineProperty(type, "_debugInfo", {
				configurable: !1,
				enumerable: !1,
				writable: !0,
				value: null
			});
			Object.defineProperty(type, "_debugStack", {
				configurable: !1,
				enumerable: !1,
				writable: !0,
				value: debugStack
			});
			Object.defineProperty(type, "_debugTask", {
				configurable: !1,
				enumerable: !1,
				writable: !0,
				value: debugTask
			});
			Object.freeze && (Object.freeze(type.props), Object.freeze(type));
			return type;
		}
		function jsxDEVImpl(type, config, maybeKey, isStaticChildren, debugStack, debugTask) {
			var children = config.children;
			if (void 0 !== children) if (isStaticChildren) if (isArrayImpl(children)) {
				for (isStaticChildren = 0; isStaticChildren < children.length; isStaticChildren++) validateChildKeys(children[isStaticChildren]);
				Object.freeze && Object.freeze(children);
			} else console.error("React.jsx: Static children should always be an array. You are likely explicitly calling React.jsxs or React.jsxDEV. Use the Babel transform instead.");
			else validateChildKeys(children);
			if (hasOwnProperty.call(config, "key")) {
				children = getComponentNameFromType(type);
				var keys = Object.keys(config).filter(function(k) {
					return "key" !== k;
				});
				isStaticChildren = 0 < keys.length ? "{key: someKey, " + keys.join(": ..., ") + ": ...}" : "{key: someKey}";
				didWarnAboutKeySpread[children + isStaticChildren] || (keys = 0 < keys.length ? "{" + keys.join(": ..., ") + ": ...}" : "{}", console.error("A props object containing a \"key\" prop is being spread into JSX:\n  let props = %s;\n  <%s {...props} />\nReact keys must be passed directly to JSX without using spread:\n  let props = %s;\n  <%s key={someKey} {...props} />", isStaticChildren, children, keys, children), didWarnAboutKeySpread[children + isStaticChildren] = !0);
			}
			children = null;
			void 0 !== maybeKey && (checkKeyStringCoercion(maybeKey), children = "" + maybeKey);
			hasValidKey(config) && (checkKeyStringCoercion(config.key), children = "" + config.key);
			if ("key" in config) {
				maybeKey = {};
				for (var propName in config) "key" !== propName && (maybeKey[propName] = config[propName]);
			} else maybeKey = config;
			children && defineKeyPropWarningGetter(maybeKey, "function" === typeof type ? type.displayName || type.name || "Unknown" : type);
			return ReactElement(type, children, maybeKey, getOwner(), debugStack, debugTask);
		}
		function validateChildKeys(node) {
			isValidElement(node) ? node._store && (node._store.validated = 1) : "object" === typeof node && null !== node && node.$$typeof === REACT_LAZY_TYPE && ("fulfilled" === node._payload.status ? isValidElement(node._payload.value) && node._payload.value._store && (node._payload.value._store.validated = 1) : node._store && (node._store.validated = 1));
		}
		function isValidElement(object) {
			return "object" === typeof object && null !== object && object.$$typeof === REACT_ELEMENT_TYPE;
		}
		var React = require_react(), REACT_ELEMENT_TYPE = Symbol.for("react.transitional.element"), REACT_PORTAL_TYPE = Symbol.for("react.portal"), REACT_FRAGMENT_TYPE = Symbol.for("react.fragment"), REACT_STRICT_MODE_TYPE = Symbol.for("react.strict_mode"), REACT_PROFILER_TYPE = Symbol.for("react.profiler"), REACT_CONSUMER_TYPE = Symbol.for("react.consumer"), REACT_CONTEXT_TYPE = Symbol.for("react.context"), REACT_FORWARD_REF_TYPE = Symbol.for("react.forward_ref"), REACT_SUSPENSE_TYPE = Symbol.for("react.suspense"), REACT_SUSPENSE_LIST_TYPE = Symbol.for("react.suspense_list"), REACT_MEMO_TYPE = Symbol.for("react.memo"), REACT_LAZY_TYPE = Symbol.for("react.lazy"), REACT_ACTIVITY_TYPE = Symbol.for("react.activity"), REACT_CLIENT_REFERENCE = Symbol.for("react.client.reference"), ReactSharedInternals = React.__CLIENT_INTERNALS_DO_NOT_USE_OR_WARN_USERS_THEY_CANNOT_UPGRADE, hasOwnProperty = Object.prototype.hasOwnProperty, isArrayImpl = Array.isArray, createTask = console.createTask ? console.createTask : function() {
			return null;
		};
		React = { react_stack_bottom_frame: function(callStackForError) {
			return callStackForError();
		} };
		var specialPropKeyWarningShown;
		var didWarnAboutElementRef = {};
		var unknownOwnerDebugStack = React.react_stack_bottom_frame.bind(React, UnknownOwner)();
		var unknownOwnerDebugTask = createTask(getTaskName(UnknownOwner));
		var didWarnAboutKeySpread = {};
		exports.Fragment = REACT_FRAGMENT_TYPE;
		exports.jsx = function(type, config, maybeKey) {
			var trackActualOwner = 1e4 > ReactSharedInternals.recentlyCreatedOwnerStacks++;
			return jsxDEVImpl(type, config, maybeKey, !1, trackActualOwner ? Error("react-stack-top-frame") : unknownOwnerDebugStack, trackActualOwner ? createTask(getTaskName(type)) : unknownOwnerDebugTask);
		};
		exports.jsxs = function(type, config, maybeKey) {
			var trackActualOwner = 1e4 > ReactSharedInternals.recentlyCreatedOwnerStacks++;
			return jsxDEVImpl(type, config, maybeKey, !0, trackActualOwner ? Error("react-stack-top-frame") : unknownOwnerDebugStack, trackActualOwner ? createTask(getTaskName(type)) : unknownOwnerDebugTask);
		};
	})();
}));
//#endregion
//#region node_modules/react/jsx-runtime.js
var require_jsx_runtime = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_react_jsx_runtime_development();
}));
//#endregion
//#region node_modules/clsx/dist/clsx.mjs
var import_react = require_react();
var import_jsx_runtime = require_jsx_runtime();
var import_react_dom = require_react_dom();
function r(e) {
	var t, f, n = "";
	if ("string" == typeof e || "number" == typeof e) n += e;
	else if ("object" == typeof e) if (Array.isArray(e)) {
		var o = e.length;
		for (t = 0; t < o; t++) e[t] && (f = r(e[t])) && (n && (n += " "), n += f);
	} else for (f in e) e[f] && (n && (n += " "), n += f);
	return n;
}
function clsx() {
	for (var e, t, f = 0, n = "", o = arguments.length; f < o; f++) (e = arguments[f]) && (t = r(e)) && (n && (n += " "), n += t);
	return n;
}
//#endregion
//#region node_modules/make-event-props/dist/index.js
var clipboardEvents = [
	"onCopy",
	"onCut",
	"onPaste"
];
var compositionEvents = [
	"onCompositionEnd",
	"onCompositionStart",
	"onCompositionUpdate"
];
var focusEvents = ["onFocus", "onBlur"];
var formEvents = [
	"onInput",
	"onInvalid",
	"onReset",
	"onSubmit"
];
var imageEvents = ["onLoad", "onError"];
var keyboardEvents = [
	"onKeyDown",
	"onKeyPress",
	"onKeyUp"
];
var mediaEvents = [
	"onAbort",
	"onCanPlay",
	"onCanPlayThrough",
	"onDurationChange",
	"onEmptied",
	"onEncrypted",
	"onEnded",
	"onError",
	"onLoadedData",
	"onLoadedMetadata",
	"onLoadStart",
	"onPause",
	"onPlay",
	"onPlaying",
	"onProgress",
	"onRateChange",
	"onSeeked",
	"onSeeking",
	"onStalled",
	"onSuspend",
	"onTimeUpdate",
	"onVolumeChange",
	"onWaiting"
];
var mouseEvents = [
	"onClick",
	"onContextMenu",
	"onDoubleClick",
	"onMouseDown",
	"onMouseEnter",
	"onMouseLeave",
	"onMouseMove",
	"onMouseOut",
	"onMouseOver",
	"onMouseUp"
];
var dragEvents = [
	"onDrag",
	"onDragEnd",
	"onDragEnter",
	"onDragExit",
	"onDragLeave",
	"onDragOver",
	"onDragStart",
	"onDrop"
];
var selectionEvents = ["onSelect"];
var touchEvents = [
	"onTouchCancel",
	"onTouchEnd",
	"onTouchMove",
	"onTouchStart"
];
var pointerEvents = [
	"onPointerDown",
	"onPointerMove",
	"onPointerUp",
	"onPointerCancel",
	"onGotPointerCapture",
	"onLostPointerCapture",
	"onPointerEnter",
	"onPointerLeave",
	"onPointerOver",
	"onPointerOut"
];
var uiEvents = ["onScroll"];
var wheelEvents = ["onWheel"];
var animationEvents = [
	"onAnimationStart",
	"onAnimationEnd",
	"onAnimationIteration"
];
var transitionEvents = ["onTransitionEnd"];
var otherEvents = ["onToggle"];
var changeEvents = ["onChange"];
var allEvents = [
	...clipboardEvents,
	...compositionEvents,
	...focusEvents,
	...formEvents,
	...imageEvents,
	...keyboardEvents,
	...mediaEvents,
	...mouseEvents,
	...dragEvents,
	...selectionEvents,
	...touchEvents,
	...pointerEvents,
	...uiEvents,
	...wheelEvents,
	...animationEvents,
	...transitionEvents,
	...changeEvents,
	...otherEvents
];
/**
* Returns an object with on-event callback props curried with provided args.
*
* @template ArgsType Type of arguments to curry on-event callbacks with.
* @param {PropsType} props Props passed to a component.
* @param {GetArgs<ArgsType>} [getArgs] A function that returns argument(s) on-event callbacks
*   shall be curried with.
*/
function makeEventProps(props, getArgs) {
	const eventProps = {};
	for (const eventName of allEvents) {
		const eventHandler = props[eventName];
		if (!eventHandler) continue;
		if (getArgs) eventProps[eventName] = ((event) => eventHandler(event, getArgs(eventName)));
		else eventProps[eventName] = eventHandler;
	}
	return eventProps;
}
//#endregion
//#region node_modules/@wojtekmaj/date-utils/dist/index.js
/**
* Utils
*/
function makeGetEdgeOfNeighbor(getPeriod, getEdgeOfPeriod, defaultOffset) {
	return function makeGetEdgeOfNeighborInternal(date, offset = defaultOffset) {
		return getEdgeOfPeriod(getPeriod(date) + offset);
	};
}
function makeGetEnd(getBeginOfNextPeriod) {
	return function makeGetEndInternal(date) {
		return /* @__PURE__ */ new Date(getBeginOfNextPeriod(date).getTime() - 1);
	};
}
function makeGetRange(getStart, getEnd) {
	return function makeGetRangeInternal(date) {
		return [getStart(date), getEnd(date)];
	};
}
/**
* Simple getters - getting a property of a given point in time
*/
/**
* Gets year from a given date.
*
* @param {DateLike} date Date to get year from
* @returns {number} Year
*/
function getYear(date) {
	if (date instanceof Date) return date.getFullYear();
	if (typeof date === "number") return date;
	const year = Number.parseInt(date, 10);
	if (typeof date === "string" && !Number.isNaN(year)) return year;
	throw new Error(`Failed to get year from date: ${date}.`);
}
/**
* Gets month from a given date.
*
* @param {Date} date Date to get month from
* @returns {number} Month
*/
function getMonth(date) {
	if (date instanceof Date) return date.getMonth();
	throw new Error(`Failed to get month from date: ${date}.`);
}
/**
* Gets day of the month from a given date.
*
* @param {Date} date Date to get day of the month from
* @returns {number} Day of the month
*/
function getDate(date) {
	if (date instanceof Date) return date.getDate();
	throw new Error(`Failed to get year from date: ${date}.`);
}
/**
* Gets hours from a given date.
*
* @param {Date | string} date Date to get hours from
* @returns {number} Hours
*/
function getHours(date) {
	if (date instanceof Date) return date.getHours();
	if (typeof date === "string") {
		const datePieces = date.split(":");
		if (datePieces.length >= 2) {
			const hoursString = datePieces[0];
			if (hoursString) {
				const hours = Number.parseInt(hoursString, 10);
				if (!Number.isNaN(hours)) return hours;
			}
		}
	}
	throw new Error(`Failed to get hours from date: ${date}.`);
}
/**
* Gets minutes from a given date.
*
* @param {Date | string} date Date to get minutes from
* @returns {number} Minutes
*/
function getMinutes(date) {
	if (date instanceof Date) return date.getMinutes();
	if (typeof date === "string") {
		const datePieces = date.split(":");
		if (datePieces.length >= 2) {
			const minutesString = datePieces[1] || "0";
			const minutes = Number.parseInt(minutesString, 10);
			if (!Number.isNaN(minutes)) return minutes;
		}
	}
	throw new Error(`Failed to get minutes from date: ${date}.`);
}
/**
* Gets seconds from a given date.
*
* @param {Date | string} date Date to get seconds from
* @returns {number} Seconds
*/
function getSeconds(date) {
	if (date instanceof Date) return date.getSeconds();
	if (typeof date === "string") {
		const datePieces = date.split(":");
		if (datePieces.length >= 2) {
			const secondsWithMillisecondsString = datePieces[2] || "0";
			const seconds = Number.parseInt(secondsWithMillisecondsString, 10);
			if (!Number.isNaN(seconds)) return seconds;
		}
	}
	throw new Error(`Failed to get seconds from date: ${date}.`);
}
/**
* Gets milliseconds from a given date.
*
* @param {Date | string} date Date to get milliseconds from
* @returns {number} Milliseconds
*/
function getMilliseconds(date) {
	if (date instanceof Date) return date.getMilliseconds();
	if (typeof date === "string") {
		const datePieces = date.split(":");
		if (datePieces.length >= 2) {
			const millisecondsString = (datePieces[2] || "0").split(".")[1] || "0";
			const milliseconds = Number.parseInt(millisecondsString, 10);
			if (!Number.isNaN(milliseconds)) return milliseconds;
		}
	}
	throw new Error(`Failed to get seconds from date: ${date}.`);
}
/**
* Century
*/
/**
* Gets century start date from a given date.
*
* @param {DateLike} date Date to get century start from
* @returns {Date} Century start date
*/
function getCenturyStart(date) {
	const year = getYear(date);
	const centuryStartYear = year + (-year + 1) % 100;
	const centuryStartDate = /* @__PURE__ */ new Date();
	centuryStartDate.setFullYear(centuryStartYear, 0, 1);
	centuryStartDate.setHours(0, 0, 0, 0);
	return centuryStartDate;
}
makeGetEdgeOfNeighbor(getYear, getCenturyStart, -100);
/**
* Gets century end date from a given date.
*
* @param {DateLike} date Date to get century end from
* @returns {Date} Century end date
*/
var getCenturyEnd = makeGetEnd(makeGetEdgeOfNeighbor(getYear, getCenturyStart, 100));
makeGetEdgeOfNeighbor(getYear, getCenturyEnd, -100);
makeGetEdgeOfNeighbor(getYear, getCenturyEnd, 100);
makeGetRange(getCenturyStart, getCenturyEnd);
/**
* Decade
*/
/**
* Gets decade start date from a given date.
*
* @param {DateLike} date Date to get decade start from
* @returns {Date} Decade start date
*/
function getDecadeStart(date) {
	const year = getYear(date);
	const decadeStartYear = year + (-year + 1) % 10;
	const decadeStartDate = /* @__PURE__ */ new Date();
	decadeStartDate.setFullYear(decadeStartYear, 0, 1);
	decadeStartDate.setHours(0, 0, 0, 0);
	return decadeStartDate;
}
makeGetEdgeOfNeighbor(getYear, getDecadeStart, -10);
/**
* Gets decade end date from a given date.
*
* @param {DateLike} date Date to get decade end from
* @returns {Date} Decade end date
*/
var getDecadeEnd = makeGetEnd(makeGetEdgeOfNeighbor(getYear, getDecadeStart, 10));
makeGetEdgeOfNeighbor(getYear, getDecadeEnd, -10);
makeGetEdgeOfNeighbor(getYear, getDecadeEnd, 10);
makeGetRange(getDecadeStart, getDecadeEnd);
/**
* Year
*/
/**
* Gets year start date from a given date.
*
* @param {DateLike} date Date to get year start from
* @returns {Date} Year start date
*/
function getYearStart(date) {
	const year = getYear(date);
	const yearStartDate = /* @__PURE__ */ new Date();
	yearStartDate.setFullYear(year, 0, 1);
	yearStartDate.setHours(0, 0, 0, 0);
	return yearStartDate;
}
makeGetEdgeOfNeighbor(getYear, getYearStart, -1);
/**
* Gets year end date from a given date.
*
* @param {DateLike} date Date to get year end from
* @returns {Date} Year end date
*/
var getYearEnd = makeGetEnd(makeGetEdgeOfNeighbor(getYear, getYearStart, 1));
makeGetEdgeOfNeighbor(getYear, getYearEnd, -1);
makeGetEdgeOfNeighbor(getYear, getYearEnd, 1);
makeGetRange(getYearStart, getYearEnd);
/**
* Month
*/
function makeGetEdgeOfNeighborMonth(getEdgeOfPeriod, defaultOffset) {
	return function makeGetEdgeOfNeighborMonthInternal(date, offset = defaultOffset) {
		const year = getYear(date);
		const month = getMonth(date) + offset;
		const previousPeriod = /* @__PURE__ */ new Date();
		previousPeriod.setFullYear(year, month, 1);
		previousPeriod.setHours(0, 0, 0, 0);
		return getEdgeOfPeriod(previousPeriod);
	};
}
/**
* Gets month start date from a given date.
*
* @param {DateLike} date Date to get month start from
* @returns {Date} Month start date
*/
function getMonthStart(date) {
	const year = getYear(date);
	const month = getMonth(date);
	const monthStartDate = /* @__PURE__ */ new Date();
	monthStartDate.setFullYear(year, month, 1);
	monthStartDate.setHours(0, 0, 0, 0);
	return monthStartDate;
}
makeGetEdgeOfNeighborMonth(getMonthStart, -1);
/**
* Gets month end date from a given date.
*
* @param {Date} date Date to get month end from
* @returns {Date} Month end date
*/
var getMonthEnd = makeGetEnd(makeGetEdgeOfNeighborMonth(getMonthStart, 1));
makeGetEdgeOfNeighborMonth(getMonthEnd, -1);
makeGetEdgeOfNeighborMonth(getMonthEnd, 1);
makeGetRange(getMonthStart, getMonthEnd);
/**
* Day
*/
function makeGetEdgeOfNeighborDay(getEdgeOfPeriod, defaultOffset) {
	return function makeGetEdgeOfNeighborDayInternal(date, offset = defaultOffset) {
		const year = getYear(date);
		const month = getMonth(date);
		const day = getDate(date) + offset;
		const previousPeriod = /* @__PURE__ */ new Date();
		previousPeriod.setFullYear(year, month, day);
		previousPeriod.setHours(0, 0, 0, 0);
		return getEdgeOfPeriod(previousPeriod);
	};
}
/**
* Gets day start date from a given date.
*
* @param {DateLike} date Date to get day start from
* @returns {Date} Day start date
*/
function getDayStart(date) {
	const year = getYear(date);
	const month = getMonth(date);
	const day = getDate(date);
	const dayStartDate = /* @__PURE__ */ new Date();
	dayStartDate.setFullYear(year, month, day);
	dayStartDate.setHours(0, 0, 0, 0);
	return dayStartDate;
}
makeGetEdgeOfNeighborDay(getDayStart, -1);
/**
* Gets day end date from a given date.
*
* @param {Date} date Date to get day end from
* @returns {Date} Day end date
*/
var getDayEnd = makeGetEnd(makeGetEdgeOfNeighborDay(getDayStart, 1));
makeGetEdgeOfNeighborDay(getDayEnd, -1);
makeGetEdgeOfNeighborDay(getDayEnd, 1);
makeGetRange(getDayStart, getDayEnd);
function padStart(num, val = 2) {
	const numStr = `${num}`;
	if (numStr.length >= val) return num;
	return `0000${numStr}`.slice(-val);
}
/**
* Returns local hours and minutes (hh:mm).
*
* @param {Date | string} date Date to get hours and minutes from
* @returns {string} Local hours and minutes
*/
function getHoursMinutes(date) {
	return `${padStart(getHours(date))}:${padStart(getMinutes(date))}`;
}
/**
* Returns local hours, minutes and seconds (hh:mm:ss).
*
* @param {Date | string} date Date to get hours, minutes and seconds from
* @returns {string} Local hours, minutes and seconds
*/
function getHoursMinutesSeconds(date) {
	return `${padStart(getHours(date))}:${padStart(getMinutes(date))}:${padStart(getSeconds(date))}`;
}
//#endregion
//#region node_modules/react-clock/dist/Hand.js
function Hand(_a) {
	var _b = _a.angle, angle = _b === void 0 ? 0 : _b, name = _a.name, _c = _a.length, length = _c === void 0 ? 100 : _c, _d = _a.oppositeLength, oppositeLength = _d === void 0 ? 10 : _d, _e = _a.width, width = _e === void 0 ? 1 : _e;
	return (0, import_jsx_runtime.jsx)("div", {
		className: "react-clock__hand react-clock__".concat(name, "-hand"),
		style: { transform: "rotate(".concat(angle, "deg)") },
		children: (0, import_jsx_runtime.jsx)("div", {
			className: "react-clock__hand__body react-clock__".concat(name, "-hand__body"),
			style: {
				width: "".concat(width, "px"),
				top: "".concat(50 - length / 2, "%"),
				bottom: "".concat(50 - oppositeLength / 2, "%")
			}
		})
	});
}
//#endregion
//#region node_modules/react-clock/dist/Mark.js
var Mark = (0, import_react.memo)(function Mark(_a) {
	var _b = _a.angle, angle = _b === void 0 ? 0 : _b, _c = _a.length, length = _c === void 0 ? 10 : _c, name = _a.name, _d = _a.width, width = _d === void 0 ? 1 : _d;
	return (0, import_jsx_runtime.jsx)("div", {
		className: "react-clock__mark react-clock__".concat(name, "-mark"),
		style: { transform: "rotate(".concat(angle, "deg)") },
		children: (0, import_jsx_runtime.jsx)("div", {
			className: "react-clock__mark__body react-clock__".concat(name, "-mark__body"),
			style: {
				width: "".concat(width, "px"),
				top: 0,
				bottom: "".concat(100 - length / 2, "%")
			}
		})
	});
});
//#endregion
//#region node_modules/react-clock/dist/MarkNumber.js
var MarkNumber = (0, import_react.memo)(function MarkNumber(_a) {
	var _b = _a.angle, angle = _b === void 0 ? 0 : _b, _c = _a.length, length = _c === void 0 ? 10 : _c, name = _a.name, number = _a.number;
	return (0, import_jsx_runtime.jsx)("div", {
		className: "react-clock__mark react-clock__".concat(name, "-mark"),
		style: { transform: "rotate(".concat(angle, "deg)") },
		children: (0, import_jsx_runtime.jsx)("div", {
			className: "react-clock__mark__number",
			style: {
				transform: "rotate(-".concat(angle, "deg)"),
				top: "".concat(length / 2, "%")
			},
			children: number
		})
	});
});
//#endregion
//#region node_modules/mimic-function/index.js
var copyProperty = (to, from, property, ignoreNonConfigurable) => {
	if (property === "length" || property === "prototype") return;
	if (property === "arguments" || property === "caller") return;
	const toDescriptor = Object.getOwnPropertyDescriptor(to, property);
	const fromDescriptor = Object.getOwnPropertyDescriptor(from, property);
	if (!canCopyProperty(toDescriptor, fromDescriptor) && ignoreNonConfigurable) return;
	Object.defineProperty(to, property, fromDescriptor);
};
var canCopyProperty = function(toDescriptor, fromDescriptor) {
	return toDescriptor === void 0 || toDescriptor.configurable || toDescriptor.writable === fromDescriptor.writable && toDescriptor.enumerable === fromDescriptor.enumerable && toDescriptor.configurable === fromDescriptor.configurable && (toDescriptor.writable || toDescriptor.value === fromDescriptor.value);
};
var changePrototype = (to, from) => {
	const fromPrototype = Object.getPrototypeOf(from);
	if (fromPrototype === Object.getPrototypeOf(to)) return;
	Object.setPrototypeOf(to, fromPrototype);
};
var wrappedToString = (withName, fromBody) => `/* Wrapped ${withName}*/\n${fromBody}`;
var toStringDescriptor = Object.getOwnPropertyDescriptor(Function.prototype, "toString");
var toStringName = Object.getOwnPropertyDescriptor(Function.prototype.toString, "name");
var changeToString = (to, from, name) => {
	const withName = name === "" ? "" : `with ${name.trim()}() `;
	const newToString = wrappedToString.bind(null, withName, from.toString());
	Object.defineProperty(newToString, "name", toStringName);
	const { writable, enumerable, configurable } = toStringDescriptor;
	Object.defineProperty(to, "toString", {
		value: newToString,
		writable,
		enumerable,
		configurable
	});
};
function mimicFunction(to, from, { ignoreNonConfigurable = false } = {}) {
	const { name } = to;
	for (const property of Reflect.ownKeys(from)) copyProperty(to, from, property, ignoreNonConfigurable);
	changePrototype(to, from);
	changeToString(to, from, name);
	return to;
}
//#endregion
//#region node_modules/memoize/distribution/index.js
var maxTimeoutValue = 2147483647;
var cacheStore = /* @__PURE__ */ new WeakMap();
var cacheTimerStore = /* @__PURE__ */ new WeakMap();
var cacheKeyStore = /* @__PURE__ */ new WeakMap();
function getValidCacheItem(cache, key) {
	const item = cache.get(key);
	if (!item) return;
	if (item.maxAge <= Date.now()) {
		cache.delete(key);
		return;
	}
	return item;
}
/**
[Memoize](https://en.wikipedia.org/wiki/Memoization) functions - An optimization used to speed up consecutive function calls by caching the result of calls with identical input.

@param function_ - The function to be memoized.

@example
```
import memoize from 'memoize';

let index = 0;
const counter = () => ++index;
const memoized = memoize(counter);

memoized('foo');
//=> 1

// Cached as it's the same argument
memoized('foo');
//=> 1

// Not cached anymore as the arguments changed
memoized('bar');
//=> 2

memoized('bar');
//=> 2
```
*/
function memoize(function_, { cacheKey, cache = /* @__PURE__ */ new Map(), maxAge } = {}) {
	if (maxAge === 0) return function_;
	if (typeof maxAge === "number" && Number.isFinite(maxAge)) {
		if (maxAge > maxTimeoutValue) throw new TypeError(`The \`maxAge\` option cannot exceed ${maxTimeoutValue}.`);
		if (maxAge < 0) throw new TypeError("The `maxAge` option should not be a negative number.");
	}
	const memoized = function(...arguments_) {
		const key = cacheKey ? cacheKey(arguments_) : arguments_[0];
		const cacheItem = getValidCacheItem(cache, key);
		if (cacheItem) return cacheItem.data;
		const result = function_.apply(this, arguments_);
		const computedMaxAge = typeof maxAge === "function" ? maxAge(...arguments_) : maxAge;
		if (computedMaxAge !== void 0 && computedMaxAge !== Number.POSITIVE_INFINITY) {
			if (!Number.isFinite(computedMaxAge)) throw new TypeError("The `maxAge` function must return a finite number, `0`, or `Infinity`.");
			if (computedMaxAge <= 0) return result;
			if (computedMaxAge > maxTimeoutValue) throw new TypeError(`The \`maxAge\` function result cannot exceed ${maxTimeoutValue}.`);
		}
		cache.set(key, {
			data: result,
			maxAge: computedMaxAge === void 0 || computedMaxAge === Number.POSITIVE_INFINITY ? Number.POSITIVE_INFINITY : Date.now() + computedMaxAge
		});
		if (computedMaxAge !== void 0 && computedMaxAge !== Number.POSITIVE_INFINITY) {
			const timer = setTimeout(() => {
				cache.delete(key);
				cacheTimerStore.get(memoized)?.delete(timer);
			}, computedMaxAge);
			timer.unref?.();
			const timers = cacheTimerStore.get(memoized) ?? /* @__PURE__ */ new Set();
			timers.add(timer);
			cacheTimerStore.set(memoized, timers);
		}
		return result;
	};
	mimicFunction(memoized, function_, { ignoreNonConfigurable: true });
	cacheStore.set(memoized, cache);
	cacheKeyStore.set(memoized, cacheKey ?? ((arguments_) => arguments_[0]));
	return memoized;
}
//#endregion
//#region node_modules/get-user-locale/dist/index.js
function isString(el) {
	return typeof el === "string";
}
function isUnique(el, index, arr) {
	return arr.indexOf(el) === index;
}
function isAllLowerCase(el) {
	return el.toLowerCase() === el;
}
function fixCommas(el) {
	return el.indexOf(",") === -1 ? el : el.split(",");
}
function normalizeLocale(locale) {
	if (!locale) return locale;
	if (locale === "C" || locale === "posix" || locale === "POSIX") return "en-US";
	if (locale.indexOf(".") !== -1) {
		var _a = locale.split(".")[0], actualLocale = _a === void 0 ? "" : _a;
		return normalizeLocale(actualLocale);
	}
	if (locale.indexOf("@") !== -1) {
		var _b = locale.split("@")[0], actualLocale = _b === void 0 ? "" : _b;
		return normalizeLocale(actualLocale);
	}
	if (locale.indexOf("-") === -1 || !isAllLowerCase(locale)) return locale;
	var _c = locale.split("-"), splitEl1 = _c[0], _d = _c[1], splitEl2 = _d === void 0 ? "" : _d;
	return "".concat(splitEl1, "-").concat(splitEl2.toUpperCase());
}
function getUserLocalesInternal(_a) {
	var _b = _a === void 0 ? {} : _a, _c = _b.useFallbackLocale, useFallbackLocale = _c === void 0 ? true : _c, _d = _b.fallbackLocale, fallbackLocale = _d === void 0 ? "en-US" : _d;
	var languageList = [];
	if (typeof navigator !== "undefined") {
		var rawLanguages = navigator.languages || [];
		var languages = [];
		for (var _i = 0, rawLanguages_1 = rawLanguages; _i < rawLanguages_1.length; _i++) {
			var rawLanguagesItem = rawLanguages_1[_i];
			languages = languages.concat(fixCommas(rawLanguagesItem));
		}
		var rawLanguage = navigator.language;
		var language = rawLanguage ? fixCommas(rawLanguage) : rawLanguage;
		languageList = languageList.concat(languages, language);
	}
	if (useFallbackLocale) languageList.push(fallbackLocale);
	return languageList.filter(isString).map(normalizeLocale).filter(isUnique);
}
var getUserLocales = memoize(getUserLocalesInternal, { cacheKey: JSON.stringify });
function getUserLocaleInternal(options) {
	return getUserLocales(options)[0] || null;
}
var getUserLocale = memoize(getUserLocaleInternal, { cacheKey: JSON.stringify });
//#endregion
//#region node_modules/react-clock/dist/shared/hourFormatter.js
function formatHour(locale, hour) {
	return hour.toLocaleString(locale || getUserLocale() || void 0);
}
//#endregion
//#region node_modules/react-clock/dist/shared/utils.js
function isValidNumber$1(num) {
	return num !== null && num !== false && !Number.isNaN(Number(num));
}
function safeMax$1() {
	var args = [];
	for (var _i = 0; _i < arguments.length; _i++) args[_i] = arguments[_i];
	return Math.max.apply(Math, args.filter(isValidNumber$1));
}
//#endregion
//#region node_modules/react-clock/dist/Clock.js
/**
* Displays a complete clock.
*/
function Clock(_a) {
	var className = _a.className, _b = _a.formatHour, formatHour$1 = _b === void 0 ? formatHour : _b, _c = _a.hourHandLength, hourHandLength = _c === void 0 ? 50 : _c, hourHandOppositeLength = _a.hourHandOppositeLength, _d = _a.hourHandWidth, hourHandWidth = _d === void 0 ? 4 : _d, _e = _a.hourMarksLength, hourMarksLength = _e === void 0 ? 10 : _e, _f = _a.hourMarksWidth, hourMarksWidth = _f === void 0 ? 3 : _f, locale = _a.locale, _g = _a.minuteHandLength, minuteHandLength = _g === void 0 ? 70 : _g, minuteHandOppositeLength = _a.minuteHandOppositeLength, _h = _a.minuteHandWidth, minuteHandWidth = _h === void 0 ? 2 : _h, _j = _a.minuteMarksLength, minuteMarksLength = _j === void 0 ? 6 : _j, _k = _a.minuteMarksWidth, minuteMarksWidth = _k === void 0 ? 1 : _k, _l = _a.renderHourMarks, renderHourMarks = _l === void 0 ? true : _l, _m = _a.renderMinuteHand, renderMinuteHand = _m === void 0 ? true : _m, _o = _a.renderMinuteMarks, renderMinuteMarks = _o === void 0 ? true : _o, renderNumbers = _a.renderNumbers, _p = _a.renderSecondHand, renderSecondHand = _p === void 0 ? true : _p, _q = _a.secondHandLength, secondHandLength = _q === void 0 ? 90 : _q, secondHandOppositeLength = _a.secondHandOppositeLength, _r = _a.secondHandWidth, secondHandWidth = _r === void 0 ? 1 : _r, _s = _a.size, size = _s === void 0 ? 150 : _s, useMillisecondPrecision = _a.useMillisecondPrecision, value = _a.value;
	function renderMinuteMarksFn() {
		if (!renderMinuteMarks) return null;
		var minuteMarks = [];
		for (var i = 1; i <= 60; i += 1) if (!(renderHourMarks && !(i % 5))) minuteMarks.push((0, import_jsx_runtime.jsx)(Mark, {
			angle: i * 6,
			length: minuteMarksLength,
			name: "minute",
			width: minuteMarksWidth
		}, "minute_".concat(i)));
		return minuteMarks;
	}
	function renderHourMarksFn() {
		if (!renderHourMarks) return null;
		var hourMarks = [];
		for (var i = 1; i <= 12; i += 1) hourMarks.push((0, import_jsx_runtime.jsx)(Mark, {
			angle: i * 30,
			length: hourMarksLength,
			name: "hour",
			width: hourMarksWidth
		}, "hour_".concat(i)));
		return hourMarks;
	}
	function renderNumbersFn() {
		if (!renderNumbers) return null;
		var numbers = [];
		for (var i = 1; i <= 12; i += 1) numbers.push((0, import_jsx_runtime.jsx)(MarkNumber, {
			angle: i * 30,
			length: safeMax$1(renderHourMarks && hourMarksLength, renderMinuteMarks && minuteMarksLength, 0),
			name: "number",
			number: formatHour$1(locale, i)
		}, "number_".concat(i)));
		return numbers;
	}
	function renderFace() {
		return (0, import_jsx_runtime.jsxs)("div", {
			className: "react-clock__face",
			children: [
				renderMinuteMarksFn(),
				renderHourMarksFn(),
				renderNumbersFn()
			]
		});
	}
	function renderHourHandFn() {
		return (0, import_jsx_runtime.jsx)(Hand, {
			angle: value ? getHours(value) * 30 + getMinutes(value) / 2 + getSeconds(value) / 120 + (useMillisecondPrecision ? getMilliseconds(value) / 12e4 : 0) : 0,
			length: hourHandLength,
			name: "hour",
			oppositeLength: hourHandOppositeLength,
			width: hourHandWidth
		});
	}
	function renderMinuteHandFn() {
		if (!renderMinuteHand) return null;
		return (0, import_jsx_runtime.jsx)(Hand, {
			angle: value ? getHours(value) * 360 + getMinutes(value) * 6 + getSeconds(value) / 10 + (useMillisecondPrecision ? getMilliseconds(value) / 1e4 : 0) : 0,
			length: minuteHandLength,
			name: "minute",
			oppositeLength: minuteHandOppositeLength,
			width: minuteHandWidth
		});
	}
	function renderSecondHandFn() {
		if (!renderSecondHand) return null;
		return (0, import_jsx_runtime.jsx)(Hand, {
			angle: value ? getMinutes(value) * 360 + getSeconds(value) * 6 + (useMillisecondPrecision ? getMilliseconds(value) * .006 : 0) : 0,
			length: secondHandLength,
			name: "second",
			oppositeLength: secondHandOppositeLength,
			width: secondHandWidth
		});
	}
	return (0, import_jsx_runtime.jsxs)("time", {
		className: clsx("react-clock", className),
		dateTime: value instanceof Date ? value.toLocaleTimeString("en", {
			hourCycle: "h23",
			hour: "2-digit",
			minute: renderMinuteHand ? "2-digit" : void 0,
			second: renderSecondHand ? "2-digit" : void 0
		}) : value || void 0,
		style: {
			width: size,
			height: size
		},
		children: [
			renderFace(),
			renderHourHandFn(),
			renderMinuteHandFn(),
			renderSecondHandFn()
		]
	});
}
//#endregion
//#region node_modules/react-clock/dist/index.js
var dist_default$2 = Clock;
//#endregion
//#region node_modules/detect-element-overflow/dist/index.js
function getRect(element) {
	return element.getBoundingClientRect();
}
function detectElementOverflow(element, container) {
	return {
		get collidedTop() {
			return getRect(element).top < getRect(container).top;
		},
		get collidedBottom() {
			return getRect(element).bottom > getRect(container).bottom;
		},
		get collidedLeft() {
			return getRect(element).left < getRect(container).left;
		},
		get collidedRight() {
			return getRect(element).right > getRect(container).right;
		},
		get overflowTop() {
			return getRect(container).top - getRect(element).top;
		},
		get overflowBottom() {
			return getRect(element).bottom - getRect(container).bottom;
		},
		get overflowLeft() {
			return getRect(container).left - getRect(element).left;
		},
		get overflowRight() {
			return getRect(element).right - getRect(container).right;
		}
	};
}
//#endregion
//#region node_modules/react-fit/dist/Fit.js
var import_warning = /* @__PURE__ */ __toESM((/* @__PURE__ */ __commonJSMin(((exports, module) => {
	/**
	* Similar to invariant but only logs a warning if the condition is not met.
	* This can be used to log issues in development environments in critical
	* paths. Removing the logging code for production environments will keep the
	* same logic and follow the same code paths.
	*/
	var __DEV__ = true;
	var warning = function() {};
	if (__DEV__) {
		var printWarning = function printWarning(format, args) {
			var len = arguments.length;
			args = new Array(len > 1 ? len - 1 : 0);
			for (var key = 1; key < len; key++) args[key - 1] = arguments[key];
			var argIndex = 0;
			var message = "Warning: " + format.replace(/%s/g, function() {
				return args[argIndex++];
			});
			if (typeof console !== "undefined") console.error(message);
			try {
				throw new Error(message);
			} catch (x) {}
		};
		warning = function(condition, format, args) {
			var len = arguments.length;
			args = new Array(len > 2 ? len - 2 : 0);
			for (var key = 2; key < len; key++) args[key - 2] = arguments[key];
			if (format === void 0) throw new Error("`warning(condition, format, ...args)` requires a warning message argument");
			if (!condition) printWarning.apply(null, [format].concat(args));
		};
	}
	module.exports = warning;
})))(), 1);
var __rest = function(s, e) {
	var t = {};
	for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0) t[p] = s[p];
	if (s != null && typeof Object.getOwnPropertySymbols === "function") {
		for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i])) t[p[i]] = s[p[i]];
	}
	return t;
};
var isMutationObserverSupported = typeof window !== "undefined" && "MutationObserver" in window;
function capitalize(string) {
	return string.charAt(0).toUpperCase() + string.slice(1);
}
function findScrollContainer(element) {
	let parent = element.parentElement;
	while (parent) {
		const { overflow } = window.getComputedStyle(parent);
		if (overflow.split(" ").every((o) => o === "auto" || o === "scroll")) return parent;
		parent = parent.parentElement;
	}
	return document.documentElement;
}
function alignAxis({ axis, container, element, invertAxis, scrollContainer, secondary, spacing }) {
	const style = window.getComputedStyle(element);
	const parent = container.parentElement;
	if (!parent) return;
	const scrollContainerCollisions = detectElementOverflow(parent, scrollContainer);
	const documentCollisions = detectElementOverflow(parent, document.documentElement);
	const isX = axis === "x";
	const startProperty = isX ? "left" : "top";
	const endProperty = isX ? "right" : "bottom";
	const sizeProperty = isX ? "width" : "height";
	const overflowStartProperty = `overflow${capitalize(startProperty)}`;
	const overflowEndProperty = `overflow${capitalize(endProperty)}`;
	const scrollProperty = `scroll${capitalize(startProperty)}`;
	const uppercasedSizeProperty = capitalize(sizeProperty);
	const offsetSizeProperty = `offset${uppercasedSizeProperty}`;
	const clientSizeProperty = `client${uppercasedSizeProperty}`;
	const minSizeProperty = `min-${sizeProperty}`;
	const scrollbarWidth = scrollContainer[offsetSizeProperty] - scrollContainer[clientSizeProperty];
	const startSpacing = typeof spacing === "object" ? spacing[startProperty] : spacing;
	let availableStartSpace = -Math.max(scrollContainerCollisions[overflowStartProperty], documentCollisions[overflowStartProperty] + document.documentElement[scrollProperty]) - startSpacing;
	const endSpacing = typeof spacing === "object" ? spacing[endProperty] : spacing;
	let availableEndSpace = -Math.max(scrollContainerCollisions[overflowEndProperty], documentCollisions[overflowEndProperty] - document.documentElement[scrollProperty]) - endSpacing - scrollbarWidth;
	if (secondary) {
		availableStartSpace += parent[clientSizeProperty];
		availableEndSpace += parent[clientSizeProperty];
	}
	const offsetSize = element[offsetSizeProperty];
	function displayStart() {
		element.style[startProperty] = "auto";
		element.style[endProperty] = secondary ? "0" : "100%";
	}
	function displayEnd() {
		element.style[startProperty] = secondary ? "0" : "100%";
		element.style[endProperty] = "auto";
	}
	function displayIfFits(availableSpace, display) {
		const fits = offsetSize <= availableSpace;
		if (fits) display();
		return fits;
	}
	function displayStartIfFits() {
		return displayIfFits(availableStartSpace, displayStart);
	}
	function displayEndIfFits() {
		return displayIfFits(availableEndSpace, displayEnd);
	}
	function displayWhereverShrinkedFits() {
		const moreSpaceStart = availableStartSpace > availableEndSpace;
		const rawMinSize = style.getPropertyValue(minSizeProperty);
		const minSize = rawMinSize ? Number.parseInt(rawMinSize, 10) : null;
		function shrinkToSize(size) {
			(0, import_warning.default)(!minSize || size >= minSize, `<Fit />'s child will not fit anywhere with its current ${minSizeProperty} of ${minSize}px.`);
			const newSize = Math.max(size, minSize || 0);
			(0, import_warning.default)(false, `<Fit />'s child needed to have its ${sizeProperty} decreased to ${newSize}px.`);
			element.style[sizeProperty] = `${newSize}px`;
		}
		if (moreSpaceStart) {
			shrinkToSize(availableStartSpace);
			displayStart();
		} else {
			shrinkToSize(availableEndSpace);
			displayEnd();
		}
	}
	let fits;
	if (invertAxis) fits = displayStartIfFits() || displayEndIfFits();
	else fits = displayEndIfFits() || displayStartIfFits();
	if (!fits) displayWhereverShrinkedFits();
}
function alignMainAxis(args) {
	alignAxis(args);
}
function alignSecondaryAxis(args) {
	alignAxis(Object.assign(Object.assign({}, args), {
		axis: args.axis === "x" ? "y" : "x",
		secondary: true
	}));
}
function alignBothAxis(args) {
	const { invertAxis, invertSecondaryAxis } = args, commonArgs = __rest(args, ["invertAxis", "invertSecondaryAxis"]);
	alignMainAxis(Object.assign(Object.assign({}, commonArgs), { invertAxis }));
	alignSecondaryAxis(Object.assign(Object.assign({}, commonArgs), { invertAxis: invertSecondaryAxis }));
}
function Fit({ children, invertAxis, invertSecondaryAxis, mainAxis = "y", spacing = 8 }) {
	const container = (0, import_react.useRef)(void 0);
	const element = (0, import_react.useRef)(void 0);
	const elementWidth = (0, import_react.useRef)(void 0);
	const elementHeight = (0, import_react.useRef)(void 0);
	const scrollContainer = (0, import_react.useRef)(void 0);
	const fit = (0, import_react.useCallback)(() => {
		if (!scrollContainer.current || !container.current || !element.current) return;
		const currentElementWidth = element.current.clientWidth;
		const currentElementHeight = element.current.clientHeight;
		if (elementWidth.current === currentElementWidth && elementHeight.current === currentElementHeight) return;
		elementWidth.current = currentElementWidth;
		elementHeight.current = currentElementHeight;
		const parent = container.current.parentElement;
		if (!parent) return;
		const { position } = window.getComputedStyle(element.current);
		if (position !== "absolute") element.current.style.position = "absolute";
		const { position: parentPosition } = window.getComputedStyle(parent);
		if (parentPosition !== "relative" && parentPosition !== "absolute") parent.style.position = "relative";
		alignBothAxis({
			axis: mainAxis,
			container: container.current,
			element: element.current,
			invertAxis,
			invertSecondaryAxis,
			scrollContainer: scrollContainer.current,
			spacing
		});
	}, [
		invertAxis,
		invertSecondaryAxis,
		mainAxis,
		spacing
	]);
	const child = import_react.Children.only(children);
	(0, import_react.useEffect)(() => {
		fit();
		function onMutation() {
			fit();
		}
		if (isMutationObserverSupported && element.current) new MutationObserver(onMutation).observe(element.current, {
			attributes: true,
			attributeFilter: ["class", "style"]
		});
	}, [fit]);
	function assignRefs(domElement) {
		if (!domElement || !(domElement instanceof HTMLElement)) return;
		element.current = domElement;
		scrollContainer.current = findScrollContainer(domElement);
	}
	return (0, import_jsx_runtime.jsx)("span", {
		ref: (domContainer) => {
			if (!domContainer) return;
			container.current = domContainer;
			assignRefs(domContainer === null || domContainer === void 0 ? void 0 : domContainer.firstElementChild);
		},
		style: { display: "contents" },
		children: child
	});
}
//#endregion
//#region node_modules/react-fit/dist/index.js
var dist_default$1 = Fit;
//#endregion
//#region node_modules/react-time-picker/dist/Divider.js
function Divider({ children }) {
	return (0, import_jsx_runtime.jsx)("span", {
		className: "react-time-picker__inputGroup__divider",
		"data-testid": "divider",
		children
	});
}
//#endregion
//#region node_modules/react-time-picker/dist/shared/dates.js
function convert12to24(hour12, amPm) {
	let hour24 = Number(hour12);
	if (amPm === "am" && hour24 === 12) hour24 = 0;
	else if (amPm === "pm" && hour24 < 12) hour24 += 12;
	return hour24;
}
function convert24to12(hour24) {
	return [Number(hour24) % 12 || 12, Number(hour24) < 12 ? "am" : "pm"];
}
//#endregion
//#region node_modules/react-time-picker/dist/shared/dateFormatter.js
var formatterCache = /* @__PURE__ */ new Map();
function getFormatter(options) {
	return function formatter(locale, date) {
		const localeWithDefault = locale || getUserLocale();
		if (!formatterCache.has(localeWithDefault)) formatterCache.set(localeWithDefault, /* @__PURE__ */ new Map());
		const formatterCacheLocale = formatterCache.get(localeWithDefault);
		if (!formatterCacheLocale.has(options)) {
			const formatter = new Intl.DateTimeFormat(localeWithDefault || void 0, options);
			formatterCacheLocale.set(options, formatter.format.bind(formatter));
		}
		return formatterCacheLocale.get(options)(date);
	};
}
var numberFormatterCache = /* @__PURE__ */ new Map();
function getNumberFormatter(options) {
	return (locale, number) => {
		const localeWithDefault = locale || getUserLocale();
		if (!numberFormatterCache.has(localeWithDefault)) numberFormatterCache.set(localeWithDefault, /* @__PURE__ */ new Map());
		const numberFormatterCacheLocale = numberFormatterCache.get(localeWithDefault);
		if (!numberFormatterCacheLocale.has(options)) {
			const formatter = new Intl.NumberFormat(localeWithDefault || void 0, options);
			numberFormatterCacheLocale.set(options, formatter.format.bind(formatter));
		}
		return numberFormatterCacheLocale.get(options)(number);
	};
}
//#endregion
//#region node_modules/react-time-picker/dist/shared/utils.js
var ninesRegExp = new RegExp(`[${["9", "٩"].join("")}]`);
var amPmFormatter = getFormatter({ hour: "numeric" });
function getAmPmLabels(locale) {
	const amString = amPmFormatter(locale, new Date(2017, 0, 1, 9));
	const pmString = amPmFormatter(locale, new Date(2017, 0, 1, 21));
	const [am1, am2] = amString.split(ninesRegExp);
	const [pm1, pm2] = pmString.split(ninesRegExp);
	if (pm2 !== void 0) {
		if (am1 !== pm1) return [am1, pm1].map((el) => el.trim());
		if (am2 !== pm2) return [am2, pm2].map((el) => el.trim());
	}
	return ["AM", "PM"];
}
function isValidNumber(num) {
	return num !== null && num !== false && !Number.isNaN(Number(num));
}
function safeMin(...args) {
	return Math.min(...args.filter(isValidNumber));
}
function safeMax(...args) {
	return Math.max(...args.filter(isValidNumber));
}
//#endregion
//#region node_modules/react-time-picker/dist/TimeInput/AmPm.js
function AmPm({ ariaLabel, autoFocus, className, disabled, inputRef, locale, maxTime, minTime, onChange, onKeyDown, required, value }) {
	const amDisabled = minTime ? convert24to12(getHours(minTime))[1] === "pm" : false;
	const pmDisabled = maxTime ? convert24to12(getHours(maxTime))[1] === "am" : false;
	const name = "amPm";
	const [amLabel, pmLabel] = getAmPmLabels(locale);
	return (0, import_jsx_runtime.jsxs)("select", {
		"aria-label": ariaLabel,
		autoFocus,
		className: clsx(`${className}__input`, `${className}__${name}`),
		"data-input": "true",
		"data-select": "true",
		disabled,
		name,
		onChange,
		onKeyDown,
		ref: inputRef,
		required,
		value: value !== null ? value : "",
		children: [
			!value && (0, import_jsx_runtime.jsx)("option", {
				value: "",
				children: "--"
			}),
			(0, import_jsx_runtime.jsx)("option", {
				disabled: amDisabled,
				value: "am",
				children: amLabel
			}),
			(0, import_jsx_runtime.jsx)("option", {
				disabled: pmDisabled,
				value: "pm",
				children: pmLabel
			})
		]
	});
}
//#endregion
//#region node_modules/update-input-width/dist/esm/index.js
var allowedVariants = ["normal", "small-caps"];
/**
* Gets font CSS shorthand property given element.
*
* @param {HTMLElement} element Element to get font CSS shorthand property from
*/
function getFontShorthand(element) {
	if (!element) return "";
	var style = window.getComputedStyle(element);
	if (style.font) return style.font;
	if (!(style.fontFamily !== "")) return "";
	var fontVariant = allowedVariants.includes(style.fontVariant) ? style.fontVariant : "normal";
	return "".concat(style.fontStyle, " ").concat(fontVariant, " ").concat(style.fontWeight, " ").concat(style.fontSize, " / ").concat(style.lineHeight, " ").concat(style.fontFamily);
}
var cachedCanvas;
/**
* Measures text width given text and font CSS shorthand.
*
* @param {string} text Text to measure
* @param {string} font Font to use when measuring the text
*/
function measureText(text, font) {
	var context = (cachedCanvas || (cachedCanvas = document.createElement("canvas"))).getContext("2d");
	if (!context) return null;
	context.font = font;
	var width = context.measureText(text).width;
	return Math.ceil(width);
}
/**
* Updates input element width to fit its content given input element
* @param {HTMLInputElement} element
*/
function updateInputWidth(element) {
	if (typeof document === "undefined" || !element) return null;
	var font = getFontShorthand(element);
	var width = measureText(element.value || element.placeholder, font);
	if (width === null) return null;
	element.style.width = "".concat(width, "px");
	return width;
}
//#endregion
//#region node_modules/react-time-picker/dist/TimeInput/Input.js
var isBrowser = typeof window !== "undefined";
var useIsomorphicLayoutEffect = isBrowser ? import_react.useLayoutEffect : import_react.useEffect;
var isIEOrEdgeLegacy = isBrowser && /(MSIE|Trident\/|Edge\/)/.test(navigator.userAgent);
var isFirefox = isBrowser && /Firefox/.test(navigator.userAgent);
function onFocus(event) {
	const { target } = event;
	if (isIEOrEdgeLegacy) requestAnimationFrame(() => target.select());
	else target.select();
}
function updateInputWidthOnLoad(element) {
	if (document.readyState === "complete") return;
	function onLoad() {
		updateInputWidth(element);
	}
	window.addEventListener("load", onLoad);
}
function updateInputWidthOnFontLoad(element) {
	if (!document.fonts) return;
	const font = getFontShorthand(element);
	if (!font) return;
	if (document.fonts.check(font)) return;
	function onLoadingDone() {
		updateInputWidth(element);
	}
	document.fonts.addEventListener("loadingdone", onLoadingDone);
}
function getSelectionString(input) {
	/**
	* window.getSelection().toString() returns empty string in IE11 and Firefox,
	* so alternatives come first.
	*/
	if (input && "selectionStart" in input && input.selectionStart !== null && "selectionEnd" in input && input.selectionEnd !== null) return input.value.slice(input.selectionStart, input.selectionEnd);
	if ("getSelection" in window) {
		const selection = window.getSelection();
		return selection === null || selection === void 0 ? void 0 : selection.toString();
	}
	return null;
}
function makeOnKeyPress(maxLength) {
	if (maxLength === null) return;
	/**
	* Prevents keystrokes that would not produce a number or when value after keystroke would
	* exceed maxLength.
	*/
	return function onKeyPress(event) {
		if (isFirefox) return;
		const { key, target: input } = event;
		const { value } = input;
		const isNumberKey = key.length === 1 && /\d/.test(key);
		const selection = getSelectionString(input);
		if (!isNumberKey || !(selection || value.length < maxLength)) event.preventDefault();
	};
}
function Input({ ariaLabel, autoFocus, className, disabled, inputRef, max, min, name, nameForClass, onChange, onKeyDown, onKeyUp, placeholder = "--", required, showLeadingZeros, step, value }) {
	useIsomorphicLayoutEffect(() => {
		if (!inputRef || !inputRef.current) return;
		updateInputWidth(inputRef.current);
		updateInputWidthOnLoad(inputRef.current);
		updateInputWidthOnFontLoad(inputRef.current);
	}, [inputRef, value]);
	const hasLeadingZero = showLeadingZeros && value && Number(value) < 10 && (value === "0" || !value.toString().startsWith("0"));
	const maxLength = max ? max.toString().length : null;
	return (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [hasLeadingZero ? (0, import_jsx_runtime.jsx)("span", {
		className: `${className}__leadingZero`,
		children: "0"
	}) : null, (0, import_jsx_runtime.jsx)("input", {
		"aria-label": ariaLabel,
		autoComplete: "off",
		autoFocus,
		className: clsx(`${className}__input`, `${className}__${nameForClass || name}`, hasLeadingZero && `${className}__input--hasLeadingZero`),
		"data-input": "true",
		disabled,
		inputMode: "numeric",
		max,
		min,
		name,
		onChange,
		onFocus,
		onKeyDown,
		onKeyPress: makeOnKeyPress(maxLength),
		onKeyUp: (event) => {
			updateInputWidth(event.target);
			if (onKeyUp) onKeyUp(event);
		},
		placeholder,
		ref: inputRef,
		required,
		step,
		type: "number",
		value: value !== null ? value : ""
	})] });
}
//#endregion
//#region node_modules/react-time-picker/dist/TimeInput/Hour12Input.js
function Hour12Input({ amPm, maxTime, minTime, value, ...otherProps }) {
	return (0, import_jsx_runtime.jsx)(Input, {
		max: safeMin(12, maxTime && (() => {
			const [maxHourResult, maxAmPm] = convert24to12(getHours(maxTime));
			if (maxAmPm !== amPm) return null;
			return maxHourResult;
		})()),
		min: safeMax(1, minTime && (() => {
			const [minHourResult, minAmPm] = convert24to12(getHours(minTime));
			if (minAmPm !== amPm || minHourResult === 12) return null;
			return minHourResult;
		})()),
		name: "hour12",
		nameForClass: "hour",
		value: value ? convert24to12(value)[0].toString() : "",
		...otherProps
	});
}
//#endregion
//#region node_modules/react-time-picker/dist/TimeInput/Hour24Input.js
function Hour24Input({ maxTime, minTime, ...otherProps }) {
	return (0, import_jsx_runtime.jsx)(Input, {
		max: safeMin(23, maxTime && getHours(maxTime)),
		min: safeMax(0, minTime && getHours(minTime)),
		name: "hour24",
		nameForClass: "hour",
		...otherProps
	});
}
//#endregion
//#region node_modules/react-time-picker/dist/TimeInput/MinuteInput.js
function MinuteInput({ hour, maxTime, minTime, showLeadingZeros = true, ...otherProps }) {
	function isSameHour(date) {
		return hour === getHours(date).toString();
	}
	return (0, import_jsx_runtime.jsx)(Input, {
		max: safeMin(59, maxTime && isSameHour(maxTime) && getMinutes(maxTime)),
		min: safeMax(0, minTime && isSameHour(minTime) && getMinutes(minTime)),
		name: "minute",
		showLeadingZeros,
		...otherProps
	});
}
//#endregion
//#region node_modules/react-time-picker/dist/TimeInput/NativeInput.js
function NativeInput({ ariaLabel, disabled, maxTime, minTime, name, onChange, required, value, valueType }) {
	const nativeValueParser = (() => {
		switch (valueType) {
			case "hour": return (receivedValue) => `${getHours(receivedValue)}:00`;
			case "minute": return getHoursMinutes;
			case "second": return getHoursMinutesSeconds;
			default: throw new Error("Invalid valueType");
		}
	})();
	const step = (() => {
		switch (valueType) {
			case "hour": return 3600;
			case "minute": return 60;
			case "second": return 1;
			default: throw new Error("Invalid valueType");
		}
	})();
	function stopPropagation(event) {
		event.stopPropagation();
	}
	return (0, import_jsx_runtime.jsx)("input", {
		"aria-label": ariaLabel,
		disabled,
		hidden: true,
		max: maxTime ? nativeValueParser(maxTime) : void 0,
		min: minTime ? nativeValueParser(minTime) : void 0,
		name,
		onChange,
		onFocus: stopPropagation,
		required,
		step,
		style: {
			visibility: "hidden",
			position: "absolute",
			zIndex: "-999"
		},
		type: "time",
		value: value ? nativeValueParser(value) : ""
	});
}
//#endregion
//#region node_modules/react-time-picker/dist/TimeInput/SecondInput.js
function SecondInput({ hour, maxTime, minTime, minute, showLeadingZeros = true, ...otherProps }) {
	function isSameMinute(date) {
		return hour === getHours(date).toString() && minute === getMinutes(date).toString();
	}
	return (0, import_jsx_runtime.jsx)(Input, {
		max: safeMin(59, maxTime && isSameMinute(maxTime) && getSeconds(maxTime)),
		min: safeMax(0, minTime && isSameMinute(minTime) && getSeconds(minTime)),
		name: "second",
		showLeadingZeros,
		...otherProps
	});
}
//#endregion
//#region node_modules/react-time-picker/dist/TimeInput.js
var getFormatterOptionsCache = {};
var allViews = [
	"hour",
	"minute",
	"second"
];
function isInternalInput(element) {
	return element.dataset.input === "true";
}
function findInput(element, property) {
	let nextElement = element;
	do
		nextElement = nextElement[property];
	while (nextElement && !isInternalInput(nextElement));
	return nextElement;
}
function focus(element) {
	if (element) element.focus();
}
function renderCustomInputs(placeholder, elementFunctions, allowMultipleInstances) {
	const usedFunctions = [];
	const pattern = new RegExp(Object.keys(elementFunctions).map((el) => `${el}+`).join("|"), "g");
	const matches = placeholder.match(pattern);
	return placeholder.split(pattern).reduce((arr, element, index) => {
		const divider = element && (0, import_jsx_runtime.jsx)(Divider, { children: element }, `separator_${index}`);
		arr.push(divider);
		const currentMatch = matches === null || matches === void 0 ? void 0 : matches[index];
		if (currentMatch) {
			const renderFunction = elementFunctions[currentMatch] || elementFunctions[Object.keys(elementFunctions).find((elementFunction) => currentMatch.match(elementFunction))];
			if (!renderFunction) return arr;
			if (!allowMultipleInstances && usedFunctions.includes(renderFunction)) arr.push(currentMatch);
			else {
				arr.push(renderFunction(currentMatch, index));
				usedFunctions.push(renderFunction);
			}
		}
		return arr;
	}, []);
}
var formatNumber = getNumberFormatter({ useGrouping: false });
function TimeInput({ amPmAriaLabel, autoFocus, className, disabled, format, hourAriaLabel, hourPlaceholder, isClockOpen: isClockOpenProps = null, locale, maxDetail = "minute", maxTime, minTime, minuteAriaLabel, minutePlaceholder, name = "time", nativeInputAriaLabel, onChange: onChangeProps, onInvalidChange, required, secondAriaLabel, secondPlaceholder, value: valueProps }) {
	const [amPm, setAmPm] = (0, import_react.useState)(null);
	const [hour, setHour] = (0, import_react.useState)(null);
	const [minute, setMinute] = (0, import_react.useState)(null);
	const [second, setSecond] = (0, import_react.useState)(null);
	const [value, setValue] = (0, import_react.useState)(null);
	const amPmInput = (0, import_react.useRef)(null);
	const hour12Input = (0, import_react.useRef)(null);
	const hour24Input = (0, import_react.useRef)(null);
	const minuteInput = (0, import_react.useRef)(null);
	const secondInput = (0, import_react.useRef)(null);
	const [isClockOpen, setIsClockOpen] = (0, import_react.useState)(isClockOpenProps);
	const lastPressedKey = (0, import_react.useRef)(void 0);
	(0, import_react.useEffect)(() => {
		setIsClockOpen(isClockOpenProps);
	}, [isClockOpenProps]);
	(0, import_react.useEffect)(() => {
		const nextValue = valueProps;
		if (nextValue) {
			setAmPm(convert24to12(getHours(nextValue))[1]);
			setHour(getHours(nextValue).toString());
			setMinute(getMinutes(nextValue).toString());
			setSecond(getSeconds(nextValue).toString());
			setValue(nextValue);
		} else {
			setAmPm(null);
			setHour(null);
			setMinute(null);
			setSecond(null);
			setValue(null);
		}
	}, [
		valueProps,
		minTime,
		maxTime,
		maxDetail,
		isClockOpen
	]);
	const valueType = maxDetail;
	const formatTime = (() => {
		const level = allViews.indexOf(maxDetail);
		return getFormatter(getFormatterOptionsCache[level] || (() => {
			const options = { hour: "numeric" };
			if (level >= 1) options.minute = "numeric";
			if (level >= 2) options.second = "numeric";
			getFormatterOptionsCache[level] = options;
			return options;
		})());
	})();
	/**
	* Gets current value in a desired format.
	*/
	function getProcessedValue(value) {
		return (() => {
			switch (valueType) {
				case "hour":
				case "minute": return getHoursMinutes;
				case "second": return getHoursMinutesSeconds;
				default: throw new Error("Invalid valueType");
			}
		})()(value);
	}
	const placeholder = format || (() => {
		const hour24 = 21;
		const hour12 = 9;
		const minute = 13;
		const second = 14;
		return formatTime(locale, new Date(2017, 0, 1, hour24, minute, second)).replace(formatNumber(locale, hour12), "h").replace(formatNumber(locale, hour24), "H").replace(formatNumber(locale, minute), "mm").replace(formatNumber(locale, second), "ss").replace(new RegExp(getAmPmLabels(locale).join("|")), "a");
	})();
	const divider = (() => {
		const dividers = placeholder.match(/[^0-9a-z]/i);
		return dividers ? dividers[0] : null;
	})();
	function onClick(event) {
		if (event.target === event.currentTarget) {
			const firstInput = event.target.children[1];
			focus(firstInput);
		}
	}
	function onKeyDown(event) {
		lastPressedKey.current = event.key;
		switch (event.key) {
			case "ArrowLeft":
			case "ArrowRight":
			case divider: {
				event.preventDefault();
				const { target: input } = event;
				focus(findInput(input, event.key === "ArrowLeft" ? "previousElementSibling" : "nextElementSibling"));
				break;
			}
			default:
		}
	}
	function onKeyUp(event) {
		const { key, target: input } = event;
		if (!(lastPressedKey.current === key)) return;
		if (!!Number.isNaN(Number(key))) return;
		const max = input.getAttribute("max");
		if (!max) return;
		const { value } = input;
		/**
		* Given 1, the smallest possible number the user could type by adding another digit is 10.
		* 10 would be a valid value given max = 12, so we won't jump to the next input.
		* However, given 2, smallers possible number would be 20, and thus keeping the focus in
		* this field doesn't make sense.
		*/
		if (Number(value) * 10 > Number(max) || value.length >= max.length) focus(findInput(input, "nextElementSibling"));
	}
	/**
	* Called after internal onChange. Checks input validity. If all fields are valid,
	* calls props.onChange.
	*/
	function onChangeExternal() {
		if (!onChangeProps) return;
		function filterBoolean(value) {
			return Boolean(value);
		}
		const formElements = [
			amPmInput.current,
			hour12Input.current,
			hour24Input.current,
			minuteInput.current,
			secondInput.current
		].filter(filterBoolean);
		const formElementsWithoutSelect = formElements.slice(1);
		const values = {};
		for (const formElement of formElements) values[formElement.name] = formElement.type === "number" ? formElement.valueAsNumber : formElement.value;
		if (formElementsWithoutSelect.every((formElement) => !formElement.value)) {
			onChangeProps(null, false);
			return;
		}
		const isEveryValueFilled = formElements.every((formElement) => formElement.value);
		const isEveryValueValid = formElements.every((formElement) => formElement.validity.valid);
		if (isEveryValueFilled && isEveryValueValid) {
			const hour = Number(values.hour24 || values.hour12 && values.amPm && convert12to24(values.hour12, values.amPm) || 0);
			const minute = Number(values.minute || 0);
			const second = Number(values.second || 0);
			const padStart = (num) => `0${num}`.slice(-2);
			onChangeProps(getProcessedValue(`${padStart(hour)}:${padStart(minute)}:${padStart(second)}`), false);
			return;
		}
		if (!onInvalidChange) return;
		onInvalidChange();
	}
	/**
	* Called when non-native date input is changed.
	*/
	function onChange(event) {
		const { name, value } = event.target;
		switch (name) {
			case "amPm":
				setAmPm(value);
				break;
			case "hour12":
				setHour(value ? convert12to24(value, amPm || "am").toString() : "");
				break;
			case "hour24":
				setHour(value);
				break;
			case "minute":
				setMinute(value);
				break;
			case "second":
				setSecond(value);
				break;
		}
		onChangeExternal();
	}
	/**
	* Called when native date input is changed.
	*/
	function onChangeNative(event) {
		const { value } = event.target;
		if (!onChangeProps) return;
		onChangeProps(value || null, false);
	}
	const commonInputProps = {
		className,
		disabled,
		maxTime,
		minTime,
		onChange,
		onKeyDown,
		onKeyUp,
		required: Boolean(required || isClockOpen)
	};
	function renderHour12(currentMatch, index) {
		if (currentMatch && currentMatch.length > 2) throw new Error(`Unsupported token: ${currentMatch}`);
		const showLeadingZeros = currentMatch ? currentMatch.length === 2 : false;
		return (0, import_jsx_runtime.jsx)(Hour12Input, {
			...commonInputProps,
			amPm,
			ariaLabel: hourAriaLabel,
			autoFocus: index === 0 && autoFocus,
			inputRef: hour12Input,
			placeholder: hourPlaceholder,
			showLeadingZeros,
			value: hour
		}, "hour12");
	}
	function renderHour24(currentMatch, index) {
		if (currentMatch && currentMatch.length > 2) throw new Error(`Unsupported token: ${currentMatch}`);
		const showLeadingZeros = currentMatch ? currentMatch.length === 2 : false;
		return (0, import_jsx_runtime.jsx)(Hour24Input, {
			...commonInputProps,
			ariaLabel: hourAriaLabel,
			autoFocus: index === 0 && autoFocus,
			inputRef: hour24Input,
			placeholder: hourPlaceholder,
			showLeadingZeros,
			value: hour
		}, "hour24");
	}
	function renderHour(currentMatch, index) {
		if (/h/.test(currentMatch)) return renderHour12(currentMatch, index);
		return renderHour24(currentMatch, index);
	}
	function renderMinute(currentMatch, index) {
		if (currentMatch && currentMatch.length > 2) throw new Error(`Unsupported token: ${currentMatch}`);
		const showLeadingZeros = currentMatch ? currentMatch.length === 2 : false;
		return (0, import_jsx_runtime.jsx)(MinuteInput, {
			...commonInputProps,
			ariaLabel: minuteAriaLabel,
			autoFocus: index === 0 && autoFocus,
			hour,
			inputRef: minuteInput,
			placeholder: minutePlaceholder,
			showLeadingZeros,
			value: minute
		}, "minute");
	}
	function renderSecond(currentMatch, index) {
		if (currentMatch && currentMatch.length > 2) throw new Error(`Unsupported token: ${currentMatch}`);
		const showLeadingZeros = currentMatch ? currentMatch.length === 2 : true;
		return (0, import_jsx_runtime.jsx)(SecondInput, {
			...commonInputProps,
			ariaLabel: secondAriaLabel,
			autoFocus: index === 0 && autoFocus,
			hour,
			inputRef: secondInput,
			minute,
			placeholder: secondPlaceholder,
			showLeadingZeros,
			value: second
		}, "second");
	}
	function renderAmPm(_currentMatch, index) {
		return (0, import_jsx_runtime.jsx)(AmPm, {
			...commonInputProps,
			ariaLabel: amPmAriaLabel,
			autoFocus: index === 0 && autoFocus,
			inputRef: amPmInput,
			locale,
			onChange,
			value: amPm
		}, "ampm");
	}
	function renderCustomInputsInternal() {
		return renderCustomInputs(placeholder, {
			h: renderHour,
			H: renderHour,
			m: renderMinute,
			s: renderSecond,
			a: renderAmPm
		}, typeof format !== "undefined");
	}
	function renderNativeInput() {
		return (0, import_jsx_runtime.jsx)(NativeInput, {
			ariaLabel: nativeInputAriaLabel,
			disabled,
			maxTime,
			minTime,
			name,
			onChange: onChangeNative,
			required,
			value,
			valueType
		}, "time");
	}
	return (0, import_jsx_runtime.jsxs)("div", {
		className,
		onClick,
		children: [renderNativeInput(), renderCustomInputsInternal()]
	});
}
//#endregion
//#region node_modules/react-time-picker/dist/TimePicker.js
var baseClassName = "react-time-picker";
var outsideActionEvents = [
	"mousedown",
	"focusin",
	"touchstart"
];
var iconProps = {
	xmlns: "http://www.w3.org/2000/svg",
	width: 19,
	height: 19,
	viewBox: "0 0 19 19",
	stroke: "black",
	strokeWidth: 2
};
var ClockIcon = (0, import_jsx_runtime.jsxs)("svg", {
	...iconProps,
	"aria-hidden": "true",
	className: `${baseClassName}__clock-button__icon ${baseClassName}__button__icon`,
	fill: "none",
	children: [(0, import_jsx_runtime.jsx)("circle", {
		cx: "9.5",
		cy: "9.5",
		r: "7.5"
	}), (0, import_jsx_runtime.jsx)("path", { d: "M9.5 4.5 v5 h4" })]
});
var ClearIcon = (0, import_jsx_runtime.jsxs)("svg", {
	...iconProps,
	"aria-hidden": "true",
	className: `${baseClassName}__clear-button__icon ${baseClassName}__button__icon`,
	children: [(0, import_jsx_runtime.jsx)("line", {
		x1: "4",
		x2: "15",
		y1: "4",
		y2: "15"
	}), (0, import_jsx_runtime.jsx)("line", {
		x1: "15",
		x2: "4",
		y1: "4",
		y2: "15"
	})]
});
function TimePicker(props) {
	const { amPmAriaLabel, autoFocus, className, clearAriaLabel, clearIcon = ClearIcon, clockAriaLabel, clockIcon = ClockIcon, closeClock: shouldCloseClockOnSelect = true, "data-testid": dataTestid, hourAriaLabel, hourPlaceholder, disableClock, disabled, format, id, isOpen: isOpenProps = null, locale, maxTime, maxDetail = "minute", minTime, minuteAriaLabel, minutePlaceholder, name = "time", nativeInputAriaLabel, onClockClose, onClockOpen, onChange: onChangeProps, onFocus: onFocusProps, onInvalidChange, openClockOnFocus = true, required, value, secondAriaLabel, secondPlaceholder, shouldCloseClock, shouldOpenClock, ...otherProps } = props;
	const [isOpen, setIsOpen] = (0, import_react.useState)(isOpenProps);
	const wrapper = (0, import_react.useRef)(null);
	const clockWrapper = (0, import_react.useRef)(null);
	(0, import_react.useEffect)(() => {
		setIsOpen(isOpenProps);
	}, [isOpenProps]);
	function openClock({ reason }) {
		if (shouldOpenClock) {
			if (!shouldOpenClock({ reason })) return;
		}
		setIsOpen(true);
		if (onClockOpen) onClockOpen();
	}
	const closeClock = (0, import_react.useCallback)(({ reason }) => {
		if (shouldCloseClock) {
			if (!shouldCloseClock({ reason })) return;
		}
		setIsOpen(false);
		if (onClockClose) onClockClose();
	}, [onClockClose, shouldCloseClock]);
	function toggleClock() {
		if (isOpen) closeClock({ reason: "buttonClick" });
		else openClock({ reason: "buttonClick" });
	}
	function onChange(value, shouldCloseClock = shouldCloseClockOnSelect) {
		if (shouldCloseClock) closeClock({ reason: "select" });
		if (onChangeProps) onChangeProps(value);
	}
	function onFocus(event) {
		if (onFocusProps) onFocusProps(event);
		if (disabled || isOpen || !openClockOnFocus || event.target.dataset.select === "true") return;
		openClock({ reason: "focus" });
	}
	const onKeyDown = (0, import_react.useCallback)((event) => {
		if (event.key === "Escape") closeClock({ reason: "escape" });
	}, [closeClock]);
	function clear() {
		onChange(null);
	}
	function stopPropagation(event) {
		event.stopPropagation();
	}
	const onOutsideAction = (0, import_react.useCallback)((event) => {
		const { current: wrapperEl } = wrapper;
		const { current: clockWrapperEl } = clockWrapper;
		const target = "composedPath" in event ? event.composedPath()[0] : event.target;
		if (target && wrapperEl && !wrapperEl.contains(target) && (!clockWrapperEl || !clockWrapperEl.contains(target))) closeClock({ reason: "outsideAction" });
	}, [closeClock]);
	const handleOutsideActionListeners = (0, import_react.useCallback)((shouldListen = isOpen) => {
		for (const event of outsideActionEvents) if (shouldListen) document.addEventListener(event, onOutsideAction);
		else document.removeEventListener(event, onOutsideAction);
		if (shouldListen) document.addEventListener("keydown", onKeyDown);
		else document.removeEventListener("keydown", onKeyDown);
	}, [
		isOpen,
		onOutsideAction,
		onKeyDown
	]);
	(0, import_react.useEffect)(() => {
		handleOutsideActionListeners();
		return () => {
			handleOutsideActionListeners(false);
		};
	}, [handleOutsideActionListeners]);
	function renderInputs() {
		const [valueFrom] = Array.isArray(value) ? value : [value];
		const ariaLabelProps = {
			amPmAriaLabel,
			hourAriaLabel,
			minuteAriaLabel,
			nativeInputAriaLabel,
			secondAriaLabel
		};
		const placeholderProps = {
			hourPlaceholder,
			minutePlaceholder,
			secondPlaceholder
		};
		return (0, import_jsx_runtime.jsxs)("div", {
			className: `${baseClassName}__wrapper`,
			children: [
				(0, import_jsx_runtime.jsx)(TimeInput, {
					...ariaLabelProps,
					...placeholderProps,
					autoFocus,
					className: `${baseClassName}__inputGroup`,
					disabled,
					format,
					isClockOpen: isOpen,
					locale,
					maxDetail,
					maxTime,
					minTime,
					name,
					onChange,
					onInvalidChange,
					required,
					value: valueFrom
				}),
				clearIcon !== null && (0, import_jsx_runtime.jsx)("button", {
					"aria-label": clearAriaLabel,
					className: `${baseClassName}__clear-button ${baseClassName}__button`,
					"data-testid": "clear-button",
					disabled,
					onClick: clear,
					onFocus: stopPropagation,
					type: "button",
					children: typeof clearIcon === "function" ? (0, import_react.createElement)(clearIcon) : clearIcon
				}),
				clockIcon !== null && !disableClock && (0, import_jsx_runtime.jsx)("button", {
					"aria-expanded": isOpen || false,
					"aria-label": clockAriaLabel,
					className: `${baseClassName}__clock-button ${baseClassName}__button`,
					"data-testid": "clock-button",
					disabled,
					onClick: toggleClock,
					onFocus: stopPropagation,
					type: "button",
					children: typeof clockIcon === "function" ? (0, import_react.createElement)(clockIcon) : clockIcon
				})
			]
		});
	}
	function renderClock() {
		if (isOpen === null || disableClock) return null;
		const { clockProps, portalContainer, value } = props;
		const className = `${baseClassName}__clock`;
		const classNames = clsx(className, `${className}--${isOpen ? "open" : "closed"}`);
		const [valueFrom] = Array.isArray(value) ? value : [value];
		const clock = (0, import_jsx_runtime.jsx)(dist_default$2, {
			locale,
			value: valueFrom,
			...clockProps
		});
		return portalContainer ? (0, import_react_dom.createPortal)((0, import_jsx_runtime.jsx)("div", {
			ref: clockWrapper,
			className: classNames,
			children: clock
		}), portalContainer) : (0, import_jsx_runtime.jsx)(dist_default$1, { children: (0, import_jsx_runtime.jsx)("div", {
			ref: (ref) => {
				if (ref && !isOpen) ref.removeAttribute("style");
			},
			className: classNames,
			children: clock
		}) });
	}
	const eventProps = (0, import_react.useMemo)(() => makeEventProps(otherProps), [otherProps]);
	return (0, import_jsx_runtime.jsxs)("div", {
		className: clsx(baseClassName, `${baseClassName}--${isOpen ? "open" : "closed"}`, `${baseClassName}--${disabled ? "disabled" : "enabled"}`, className),
		"data-testid": dataTestid,
		id,
		...eventProps,
		onFocus,
		ref: wrapper,
		children: [renderInputs(), renderClock()]
	});
}
//#endregion
//#region node_modules/react-time-picker/dist/index.js
var dist_default = TimePicker;
//#endregion
export { TimePicker, dist_default as default };

//# sourceMappingURL=react-time-picker.js.map